# Override rate limiting for load testing.
# All k6 VUs originate from 127.0.0.1, so they share one token bucket.
# These values allow 2000-req bursts and sustain 1000 req/s from a single IP.
RATE_LIMIT_CAPACITY=2000
RATE_LIMIT_REFILL_RATE=1000
